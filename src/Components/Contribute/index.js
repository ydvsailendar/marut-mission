import React from "react";

export default function Contribute() {
  return (
    <div>
      Package managed installs don’t include documentation, but do include our
      build system and readme.
    </div>
  );
}
